version:
  version: $(version-manager --tag 0.0.0-master)
  files:
    setup.py: version='**VERSION**',
requirements:
  version: $(bin/requires_versions.py)
  files:
    setup.py: (install_requires=\[)(.*?)(\],)

